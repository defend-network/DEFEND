"""LEGACY STACK — legacy executable tools.

These are legacy/transitional tool entrypoints (DEFEND Sports, legacy TT
engine, coder diagnostic). NOT canonical. See ``legacy_stack/README.md``.

Invoke explicitly, e.g. ``python -m legacy_stack.legacy_stack.tools.defend_tt_backfill``.
"""
